# pipe-io v1.0.0 - Stable API

**Date:** 2026-05-19
**Compare:** `v0.9.0...v1.0.0`

## Headline

**The public surface is frozen.** Backwards-incompatible changes
after `1.0.0` require a major version bump per Semantic Versioning.
See [`REPS.md`](../../REPS.md) section 8 for the binding stability
policy and [`docs/API.md`](../API.md) for the complete public-symbol
reference.

No source-level changes from `v0.9.0`. `1.0.0` is a freeze, a
documentation polish, and the CI gate that protects the
guarantee going forward.

Behind the line: the journey from `v0.1.0` (scaffold) to `v1.0.0`
landed the full locked surface - `Source`, `Stage`, `Sink`, `Emit`
traits with closure adapters and custom-trait support; count /
byte / age batching with the `ByteSize` opt-in; tumbling /
sliding / session windowing behind a pluggable `Clock`;
`FailFast` / `Continue` / `DeadLetter` error policies with
shared-handle dead-letter routing; `SyncDriver` and
`ThreadedDriver`; the open `Driver` trait for custom executors;
an 84-test suite (32 unit + 42 integration + 10 doctest) covering
every surface; 11 `proptest` invariants; 4 `cargo-fuzz` harnesses;
benchmark numbers in `docs/BENCH.md`; an 11-section user guide;
8 runnable examples; a per-version migration guide; and the
`cargo-semver-checks` CI job that catches accidental breakage
on every PR.

## What changed since `v0.9.0`

### Changed - `cargo-semver-checks` CI is now a hard gate

The CI `semver-checks` job no longer runs with `|| true`.
Backwards-incompatible additions to the public surface fail CI
before they can ship.

### Changed - `REPS.md` section 8 declares stability

The section was previously "Stability begins at `1.0.0`,
governed by [...]". It now declares the public API frozen and
enumerates the patch / minor / major rules in full, plus the
`#[non_exhaustive]` reminder for `Error` and `StageFailure`.

### Changed - `REPS.md` section 7 records the measured perf contract

The placeholder ("To be specified at `0.4.0`") is replaced with
the headline numbers from `docs/BENCH.md`. The numbers are
indicative, not contractual; hardware and workloads vary.

### Changed - `REPS.md` section 4.9 drops the unimplemented `.buffer(...)`

The `.buffer(capacity)` builder method was listed in the locked
surface but never implemented. Section 4.9 records it as
post-1.0 minor-release roadmap material; section 4.10 keeps the
feature-flag table accurate.

### Changed - `ROADMAP.md` re-anchored

All `0.x` releases listed as shipped. Forward-looking work
(`.buffer`, per-stage threading, `pipe-io-tokio`,
`StageFailure::input`) explicitly marked as post-1.0 minor
releases.

### Changed - `README.md` rewritten for production

Hero block, feature highlights grouped by concern (pipeline
composition / backpressure-batching-windowing / error
isolation / runtime / reliability), installation block with
feature-flag table, quick-start snippets per surface,
examples table, status section declaring 1.0.0 stable,
documentation links, version compatibility, contributing notes,
license. Mirrors the style of the sibling `config-lib` crate.

### Changed - `docs/API.md` declared frozen

The intro now points at the `1.0.0` freeze and the
[Stability](../API.md#stability) section spells out the SemVer
rules and the `#[non_exhaustive]` reminder. The duplicated
feature-flag tables are harmonized.

### Changed - `docs/MIGRATION.md` records the `v0.9.0 -> v1.0.0` upgrade

Pure version marker; no source changes required.

There are no source-level changes to the crate code between
`v0.9.0` and `v1.0.0`. The package contents on crates.io are
functionally identical; the version bump signals API stability.

## Frozen public surface

Full listing in [`docs/API.md`](../API.md). Summary by module:

### Always available (data primitives and traits)

- Traits: `pipe_io::{Source, Stage, Sink, Emit, ByteSize, StageError, Driver}`
- Data types: `pipe_io::{Batch, BatchPolicy, Error, Result, BoxError, StageId, StageFailure, BufferErrorKind, ErrorPolicy, EmitError, RunStats, VERSION}`
- Source adapters: `pipe_io::source::{IterSource, FnSource, Infallible}`
- Sink adapters: `pipe_io::sink::{NullSink, FnSink}`
- Drivers: `pipe_io::driver::SyncDriver`
- Pipeline machinery: `pipe_io::{Pipeline, PipelineBuilder}` and the full builder method set

### `std` feature (default)

- Source adapters: `pipe_io::source::{ChannelSource, ReaderSource}`
- Sink adapters: `pipe_io::sink::{VecSink, SharedHandle, ChannelSink, ChannelSinkError, WriterSink}`
- Drivers: `pipe_io::driver::ThreadedDriver`
- `RunStats::duration: Duration` field
- `BatchPolicy::max_age` / `BatchPolicy::age_limit` triggers
- `pipe_io::window::{Clock, SystemClock, Window, WindowPolicy}` (re-exported from the crate root)
- `Pipeline::run_threaded` and `PipelineBuilder::dead_letter`

### Feature flags

| Flag    | Default | Effect                                                                                                |
|---------|---------|-------------------------------------------------------------------------------------------------------|
| `std`   | yes     | `ThreadedDriver`, `Pipeline::run_threaded`, `ChannelSource`, `ReaderSource`, `ChannelSink`, `WriterSink`, `VecSink`, `Window` / `Clock` / `SystemClock`, `BatchPolicy::max_age`, dead-letter routing. |

### MSRV

`1.75`, unchanged from `v0.9.0`. Locked from `1.0.0` onward.

### Runtime dependencies

Zero. The crate depends only on `core` and `alloc` (always)
plus `std` when the `std` feature is enabled. `proptest` is a
dev-only dependency.

## Verification

Local matrix (Windows x86_64-pc-windows-msvc, stable 1.95):

- `cargo build` (default features) [OK]
- `cargo build --no-default-features` [OK]
- `cargo build --all-features` [OK]
- `cargo build --examples` [OK]
- `cargo +1.75 build --all-features` (MSRV) [OK]
- `cargo fmt --all -- --check` [OK]
- `cargo clippy --all-targets --no-default-features -- -D warnings` [OK]
- `cargo clippy --all-targets --all-features -- -D warnings` [OK]
- `cargo test --all-features` - **84 tests pass, 0 ignored**
  (32 unit + 42 integration + 10 doctest)
- `RUSTDOCFLAGS="-D warnings -D rustdoc::broken-intra-doc-links" cargo doc --all-features --no-deps` [OK]
- `cargo package --no-verify --allow-dirty` [OK]
- Banned-word and em-dash scan across `src/`, `tests/`, `benches/`,
  `examples/`, `docs/`, `README.md`, `CHANGELOG.md`, `REPS.md`: 0 hits

CI matrix runs on ubuntu-latest, macos-latest, windows-latest
for the `test` job plus dedicated `clippy`, `fmt`, `docs`,
`msrv`, and `semver-checks` jobs. The `semver-checks` job is
now a hard gate.

## Performance

Measured on a developer laptop (Windows, x86_64, release with
`lto = "thin"`); full methodology in [`docs/BENCH.md`](../BENCH.md).

### Pipeline throughput (200,000 items per run)

| Scenario                                  | ns / item | M items / s |
|-------------------------------------------|----------:|------------:|
| raw `Iterator::map().filter()` baseline   |     ~0    |   ~2,200    |
| source -> null sink                       |      1    |     ~500    |
| source -> map -> sink                     |      3    |     ~260    |
| source -> map -> filter -> map -> sink    |      5    |     ~170    |
| source -> filter(false) -> sink           |      2    |     ~340    |
| source -> batch(100) -> sink              |      7    |     ~140    |
| source -> try_map(ok) -> sink             |      3    |     ~290    |
| source -> try_map(50% err, Continue)      |      3    |     ~325    |
| threaded driver: source -> map -> sink    |     46    |      ~21    |

Each stage edge costs roughly 1-2 ns per item - one vtable
dispatch through the boxed stage chain. The threaded driver
pays fixed thread-spawn + join costs that amortize to ~40 ns
per item for the 200,000-item sample.

## Migration

### From `v0.9.0`

No source changes required. The public API surface is unchanged.

### From earlier `0.x` releases

See [`docs/MIGRATION.md`](../MIGRATION.md) for per-version
upgrade notes. The `0.x` line was additive; no breaking source
changes shipped between releases.

## Stability commitment

From `1.0.0` forward (full policy in
[`REPS.md`](../../REPS.md) section 8):

- **Patch (`1.0.x`)** - bug fixes, doc improvements, internal
  performance work, test additions. No new public items.
- **Minor (`1.x.0`)** - pure additions to the public surface,
  new opt-in features, new variants on `#[non_exhaustive]` enums
  reserved for growth, MSRV bumps.
- **Major (`2.0.0`)** - removes, renames, or signature changes
  of public symbols, or non-opt-in runtime dependency additions.

The `cargo-semver-checks` CI job enforces this on every pull
request and every push to `main`.

`Error` and `StageFailure` are `#[non_exhaustive]`; match arms
on them must include a wildcard pattern. These enums may gain
variants in minor releases.

## Release ceremony

```bash
git tag -a v1.0.0 -m "Release v1.0.0 - Stable API"
git push origin main
git push origin v1.0.0

cargo publish

cargo search pipe-io | head -3
```

GitHub release title: `v1.0.0 - Stable API`. Not tagged as
pre-release.

## Acknowledgements

`pipe-io` reached `1.0.0` through ten `0.x` releases. The
journey:

- `0.1.0` - Scaffold; crate name reserved.
- `0.2.0` - Design lock (untagged; folded into `0.3.0`).
- `0.3.0` - Minimum viable implementation.
- `0.4.0` - Polish and benchmarking.
- `0.5.0` - Windowing.
- `0.6.0` - Dead-letter routing.
- `0.7.0` - Driver trait and custom executors.
- `0.8.0` - Examples and guide.
- `0.9.0` - Pre-1.0 stabilization.
- `1.0.0` - Stable.

The crate never had external runtime dependencies and never
carried `unsafe` code. The public-API surface declared here is
the same one that compiled and tested green across the
preceding nine feature releases.

Pre-release work was extensive; the rules from here are simple.
Bug fixes ship as patches. Additions ship as minors. Breaking
changes wait for a major.

Thank you for using `pipe-io`.

---

**Full Changelog:** https://github.com/jamesgober/pipe-io/compare/v0.9.0...v1.0.0
